node_modules/
dist/
coverage/
*.js.map
memory/

# Stream Deck plugin build output and rolling logs.
# Generic patterns so a plugin-UUID rename can never accidentally start
# committing logs — they contain local project paths and session ids.
*.sdPlugin/bin/
*.sdPlugin/logs/
